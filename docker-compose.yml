version: '3.8'

services:
  n8n_aceaperu:
    image: n8nio/n8n:latest
    container_name: n8n_aceaperu
    restart: always
    ports:
      - "5678:5678"
    volumes:
      - ./n8n_data:/home/node/.n8n
    environment:
      - N8N_HOST=localhost
      - NODE_ENV=production
      - TZ=America/Lima